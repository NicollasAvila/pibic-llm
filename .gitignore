# --- SISTEMA & PYTHON (Padrão) ---
__pycache__/
*.py[cod]
.DS_Store
Thumbs.db

# --- SEGURANÇA (Crítico) ---
.env
.env.local

# --- AMBIENTE VIRTUAL ---
.venv/
venv/
env/

# --- IDE ---
.vscode/

# --- MODELOS (Pesados) ---
*.gguf
*.bin

# --- DADOS (Onde moram os 2GB) ---
# Ignora qualquer log dentro da pasta dados
dados/*.log
dados/*.json
dados/*.txt

# EXCEÇÃO: Permite subir o arquivo de amostra (se você criou o sample.log)
!dados/sample.log

resultados/*.csv